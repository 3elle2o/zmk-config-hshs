# ... parte precedente del tuo file workflow ...

  - name: Build HSHS46 Dongle
    uses: zmkfirmware/zmk-action-workflow@v3
    with:
      board: seeeduino_xiao_ble
      shield: hshs46_dongle prospector_adapter
      # INSERISCI I PARAMETRI QUI SOTTO:
      extra-cmake-args: "-DCONFIG_ZMK_SPLIT=y -DCONFIG_ZMK_SPLIT_ROLE_CENTRAL=y -DCONFIG_ZMK_SPLIT_BLE_PERIPHERAL_COUNT=2"

# ... resto del file ...
